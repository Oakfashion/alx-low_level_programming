#include "main.h"
/**
 * put2 - print strings
 * @str: function parameter
 * Return: 0
 */

void rev_string(char *s)
{
        int i;

        for (i = 0 ; str[i] != '\0' ; i++)
        {
                _putchar(str[i]);
                i++;
        }
        _putchar('\n');
}
